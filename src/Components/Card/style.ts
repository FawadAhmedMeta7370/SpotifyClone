import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    justifyContent: 'center',
  },
  imageStyle: {
    width: 170,
    height: 170,
    marginBottom: 10,
  },
  textStyle: {
    color: 'white',
    width: 175,
    fontSize: 16,
  },
});
